javascript:(()=>{const ID='nexo-inverter-scanner',BTN='nexo-inverter-inspect',SIMPLE='nexo-inverter-simple';document.getElementById(ID)?.remove();document.getElementById(BTN)?.remove();document.getElementById(SIMPLE)?.remove();const sleep=ms=>new Promise(r=>setTimeout(r,ms)),clean=s=>(s||%27%27).replace(/\s+/g,%27 %27).trim(),icon={search:`<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="7"/><path d="m20 20-4-4"/></svg>%60,spin:%60<svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="animation:nexoSpin .8s linear infinite"><circle cx="12" cy="12" r="9" stroke-dasharray="22 35"/></svg>%60,check:%60<svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m5 12 4 4L19 6"/></svg>%60,copy:%60<svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="8" y="8" width="12" height="12" rx="2"/><path d="M16 8V6a2 2 0 0 0-2-2H6a2 2 0 0 0-2 2v8a2 2 0 0 0-2-2"/></svg>%60,close:%60<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round"><path d="M6 6l12 12M18 6 6 18"/></svg>%60};const style=document.createElement('style');style.textContent=%60@keyframes nexoSpin{to{transform:rotate(360deg)}}#${BTN},#${SIMPLE}{position:fixed;bottom:20px;z-index:2147483647;background:#08314A;color:#fff!important;border:0;border-radius:7px;padding:9px 14px;display:flex;align-items:center;gap:7px;font:600 13px Arial,sans-serif;box-shadow:0 5px 18px #0003;cursor:pointer}#${BTN}{right:176px}#${SIMPLE}{right:20px}#${BTN} svg,#${SIMPLE} svg{color:#fff!important;stroke:#fff!important}#${BTN} span,#${SIMPLE} span{color:#fff!important}#${BTN}:hover,#${SIMPLE}:hover{filter:brightness(1.12)}#${ID}{position:fixed;right:20px;bottom:20px;width:440px;z-index:2147483647;background:#fff;border:1px solid #d8dde2;border-radius:8px;box-shadow:0 8px 30px #0003;font:13px Arial,sans-serif;color:#20262b;overflow:hidden}#${ID} .head{height:42px;background:#08314A;color:#fff!important;display:flex;align-items:center;padding:0 12px;gap:8px;font-weight:600}#${ID} .head span{color:#fff!important}#${ID} .close{margin-left:auto;background:none;border:0;color:#fff!important;cursor:pointer;padding:4px;display:flex}#${ID} .close svg{stroke:#fff!important}#${ID} .status{display:flex;align-items:center;gap:8px;padding:11px 13px;border-bottom:1px solid #e5e7e9}#${ID} .pending{display:none;padding:8px 13px;border-bottom:1px solid #e5e7e9;background:#fafbfc;max-height:120px;overflow:auto;font:12px Consolas,monospace;color:#4b555c}#${ID} .pending .item{padding:2px 0}#${ID} .result{padding:10px 12px}#${ID} textarea{width:100%;height:170px;box-sizing:border-box;resize:vertical;border:1px solid #d5dadd;border-radius:5px;padding:9px;font:12px Consolas,monospace;outline:none}#${ID} .actions{display:flex;justify-content:flex-end;gap:7px;margin-top:8px}#${ID} .actions button{display:flex;align-items:center;gap:6px;border:1px solid #cdd3d7;background:#fff;border-radius:5px;padding:6px 10px;cursor:pointer;font-size:12px}#${ID} .actions button:hover{background:#f4f6f7}%60;document.head.appendChild(style);const button=document.createElement('button');button.id=BTN;button.innerHTML=%60${icon.search}<span>Inspecionar</span>%60;const simple=document.createElement('button');simple.id=SIMPLE;simple.innerHTML=%60${icon.search}<span>Coleta simples</span>%60;document.body.append(button,simple);function snapshot(){return[...document.querySelectorAll('.element-display')].flatMap(g=>{const grupo=clean(g.querySelector('.element-display-header')?.textContent||'');return[...g.querySelectorAll('.its-info > .horizontal-bar')].map(el=>{const title=el.getAttribute('title')||'',m=title.match(/^(\S+)\s*-/);return m?{grupo,id:m[1]}:null}).filter(Boolean)}).sort((a,b)=>a.id.localeCompare(b.id,undefined,{numeric:true}))}function getCurrent(grupo,id){const g=[...document.querySelectorAll('.element-display')].find(x=>clean(x.querySelector('.element-display-header')?.textContent||'')===grupo);if(!g)return null;return[...g.querySelectorAll('.its-info > .horizontal-bar')].find(el=>(el.getAttribute('title')||'').startsWith(id+' - '))}function info(el){if(!el)return null;const title=el.getAttribute('title')||'',m=title.match(/^(\S+)\s*-/);if(!m)return null;const p=title.match(/P\s*:\s*(-?\d+(?:[.,]\d+)?)/),fills=[...el.querySelectorAll('svg,[fill]')].map(x=>(x.getAttribute('fill')||'').toLowerCase());return{id:m[1],p:p?parseFloat(p[1].replace(',','.')):null,comunicacao:/COMMS\s*(?:255|2565)/i.test(title)||fills.includes('#f3c300'),falha:fills.includes('#ff0000')}}function warning(){const panel=document.querySelector('#selected-element-content');if(!panel)return null;const w=panel.querySelector('.warnings');if(!w)return'No warnings';const texto=[...w.children].map(x=>clean(x.textContent)).filter(Boolean).join(' ');return texto||clean(w.textContent)||'No warnings'}async function waitFor(fn,timeout=800,interval=30){const t=Date.now();while(Date.now()-t<timeout){const v=fn();if(v)return v;await sleep(interval)}return null}async function abrirEler(inv){const el=getCurrent(inv.grupo,inv.id);if(!el)return null;el.click();const panel=await waitFor(()=>document.querySelector('#selected-element-content'),800,30);if(!panel)return null;const motivo=warning();document.querySelector('#selected-element-header button')?.click();await waitFor(()=>!document.querySelector('#selected-element-content'),500,30);return motivo}async function confirmarComunicacao(inv){await sleep(1000);return info(getCurrent(inv.grupo,inv.id))}async function iniciar(modo){button.remove();simple.remove();const box=document.createElement('div');box.id=ID;box.innerHTML=%60<div class="head">${icon.search}<span>Scanner de Inversores · ${modo==='simples'?'Coleta simples':'Completo'}</span><button class="close" title="Fechar">${icon.close}</button></div><div class="status"><span class="status-icon">${icon.spin}</span><span class="status-text">Iniciando...</span></div><div class="pending"></div><div class="result" style="display:none"><textarea readonly></textarea><div class="actions"><button class="copy">${icon.copy}<span>Copiar</span></button></div></div>%60;document.body.appendChild(box);const status=box.querySelector('.status-text'),pending=box.querySelector('.pending'),result=box.querySelector('.result'),textarea=box.querySelector('textarea');box.querySelector('.close').onclick=()=>{box.remove();document.body.append(button,simple)};try{const inversores=snapshot(),resultado=[],pendentes=[];status.textContent=%60Verificando 0/${inversores.length}%60;for(let i=0;i<inversores.length;i++){const inv=inversores[i];status.textContent=%60Verificando ${i+1}/${inversores.length} · ${inv.id}%60;const data=info(getCurrent(inv.grupo,inv.id));if(data?.comunicacao){pendentes.push({inv,promise:confirmarComunicacao(inv)});pending.style.display='block';pending.innerHTML=pendentes.map(x=>%60<div class="item">- ${x.inv.id} Falha de comunicação · aguardando confirmação</div>%60).join('')}else if(data){if(modo==='completo'){if(data.falha){const motivo=await abrirEler(inv);if(motivo)resultado.push(%60- ${inv.id} ${motivo}%60)}else if(data.p===0||data.p===null){const motivo=await abrirEler(inv);if(motivo)resultado.push(%60- ${inv.id} ${motivo}%60)}}else if(data.p===0||data.p===null){const motivo=await abrirEler(inv);if(motivo)resultado.push(%60- ${inv.id} ${motivo}%60)}}if(i<inversores.length-1)await sleep(40)}if(pendentes.length){status.textContent=%60Verificando ${inversores.length}/${inversores.length} · confirmando comunicação%60;for(const item of pendentes){const data=await item.promise;pending.innerHTML=pendentes.filter(x=>!x.done).map(x=>%60<div class="item">- ${x.inv.id} Falha de comunicação · aguardando confirmação</div>%60).join('');item.done=true;if(data?.comunicacao){resultado.push(%60- ${item.inv.id} Falha de comunicação%60)}else if(data){if(modo==='completo'){if(data.falha){const motivo=await abrirEler(item.inv);if(motivo)resultado.push(%60- ${item.inv.id} ${motivo}%60)}else if(data.p===0||data.p===null){const motivo=await abrirEler(item.inv);if(motivo)resultado.push(%60- ${item.inv.id} ${motivo}%60)}}else if(data.p===0||data.p===null){const motivo=await abrirEler(item.inv);if(motivo)resultado.push(%60- ${item.inv.id} ${motivo}%60)}}}pending.style.display='none'}window.__resultadoInversores=resultado;const texto=resultado.join('\n');textarea.value=texto;result.style.display='block';box.querySelector('.status-icon').innerHTML=icon.check;status.textContent=resultado.length?%60Concluído · ${resultado.length} ocorrência${resultado.length===1?'':'s'}%60:'Concluído · Nenhuma ocorrência';try{await navigator.clipboard.writeText(texto)}catch{}box.querySelector('.copy').onclick=async()=>{try{await navigator.clipboard.writeText(texto);box.querySelector('.copy').innerHTML=%60${icon.check}<span>Copiado</span>%60;setTimeout(()=>{if(box.isConnected)box.querySelector('.copy').innerHTML=%60${icon.copy}<span>Copiar</span>%60},1500)}catch{textarea.select()}}}catch(e){status.textContent='Erro durante a varredura';console.error(e)}}button.onclick=()=>iniciar('completo');simple.onclick=()=>iniciar('simples')})()
